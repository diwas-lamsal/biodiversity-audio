files: 'first_steps/.+.py|infer/.+.py|train_audio/.+.py'
repos:
  - repo: https://github.com/pycqa/flake8
    rev: '7.1.1'  # pick a git hash / tag to point to
    hooks:
    - id: flake8
      args: [--config=.flake8.ini]

